
/*
(function () {
    "use strict"
   /* document.getElementById("search-block").addEventListener("click",()=>{

        var bar = document.getElementById("search-bar")
        console.log("block")
        console.log(bar.value)
        window.location.href = 'https://mainnet.blockchainos.org/api/v1/blocks/'+bar.value;
    })
    document.getElementById("search-tx").addEventListener("click",()=>{

        var bar = document.getElementById("search-bar")
        console.log("tx")
        console.log(bar.value)
        window.location.href = 'https://mainnet.blockchainos.org/api/v1/transactions/'+bar.value;
    })
    document.getElementById("search-account").addEventListener("click",()=>{

        var bar = document.getElementById("search-bar")
        console.log("account")
        console.log(bar.value)
        window.location.href = 'https://mainnet.blockchainos.org/api/v1/accounts/'+bar.value;
    })

  }());


(function () {
    fetch('https://mainnet.blockchainos.org/')
  .then(function(response) {
    return response.json();
  })
  .then(function(myJson) {
    console.log(myJson);
  });

  }());

*/